{
  "name": "@onenomad/cortex-memory-pgvector",
  "version": "0.1.0",
  "description": "Standalone hybrid-search memory backend for Cortex. Postgres + pgvector + tsvector, fused with reciprocal rank fusion. Cortex's canonical storage backend â€” supports embedded PGlite for personal installs and external Postgres for VPS / cloud deploys. Self-contained (Cortex-internal Xenova embedder; no external memory dependency).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "typecheck": "tsc --build",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.4.5",
    "@huggingface/transformers": "^4.2.0",
    "@onenomad/cortex-core": "workspace:*",
    "pg": "^8.13.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "eslint": "^9.12.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "license": "Apache-2.0",
  "author": "OneNomad LLC",
  "repository": {
    "type": "git",
    "url": "https://github.com/OneNomad-LLC/cortex.git",
    "directory": "packages/memory-pgvector"
  },
  "homepage": "https://github.com/OneNomad-LLC/cortex#readme",
  "bugs": {
    "url": "https://github.com/OneNomad-LLC/cortex/issues"
  },
  "publishConfig": {
    "access": "public"
  }
}
